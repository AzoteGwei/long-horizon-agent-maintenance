# Execution Protocol

> Generated by long-horizon-agent-maintenance.
> Keep only rules with real binding force for this project. Do not accumulate.

## Control Level

<!-- lightweight / standard / high-risk — pick one, delete the rest -->

**Current level: lightweight**

Enabled components:
- [x] RULES.md (this file)
- [x] CURRENT.md
- [ ] findings/
- [ ] sessions/
- [ ] Negative checks
- [ ] Baseline checks

Close-out trigger: after each work unit is completed.
Phase transition trigger: all positive checks for the current milestone pass.

## Close-out Protocol

Execute before ending every session:

1. Run checks: `<project test command>`
2. Update `CURRENT.md`: done, not done, blocked, evidence, next step
3. <!-- [standard+] Append session log to sessions/ -->
4. Report results; do not claim completion without fulfilling this protocol

## Rules

<!-- Examples below — replace with actual rules for this project -->

### Rule: Completion must not be self-certified by the executor

MUST verify with the output of `<test command>`.
"Task succeeded", "no errors", or verbal confirmation cannot substitute for this evidence.
Verification: check test output at close-out.
Reason: _(fill in when first violated)_

### Rule: Tests define requirements, not the other way around

MUST fix implementation code when tests fail; MUST NOT modify test expectations to make CI pass.
Verification: `git diff` test files at close-out; if expectations changed, a requirement change record is required.
Reason: _(fill in when first violated)_

### Rule: No silent scope expansion

MUST report the reason and wait for confirmation before modifying anything outside the "in progress" scope in CURRENT.md.
Verification: compare actually modified files against the planned scope at close-out.
Reason: _(fill in when first violated)_

<!-- Append new rules here. Each rule must have:
     - MUST or SHOULD + observable requirement
     - Verification method
     - Reason (a specific failure event)
     Preferences that cannot be verified by command use SHOULD, not MUST. -->
