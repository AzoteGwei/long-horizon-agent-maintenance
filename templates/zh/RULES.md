# 执行协议

> 本文件由 long-horizon-agent-maintenance 初始化生成。
> 只保留对本项目有实际约束力的规则，不要堆积。

## 控制级别

<!-- 轻量 / 标准 / 高风险，选一个，删掉其他 -->

**当前级别：轻量**

启用的组件：
- [x] RULES.md（本文件）
- [x] CURRENT.md
- [ ] findings/
- [ ] sessions/
- [ ] 反向验证检查
- [ ] 基线验证检查

收工触发条件：每个工作单元完成后。
阶段切换触发条件：当前 Milestone 的正向检查全部通过。

## 收工流程

每个 session 结束前必须执行：

1. 运行验证检查：`<项目的测试命令>`
2. 更新 `CURRENT.md`：已完成、未完成、阻塞、证据、下一步
3. <!-- [标准+] 追加 session 日志至 sessions/ -->
4. 报告结果；未满足本流程不宣称完成

## 规则

<!-- 下面是示例条目，替换为本项目的实际规则 -->

### 规则：完成状态不可由执行者自证

MUST 以 `<测试命令>` 的通过结果复核。
"任务成功"、"无报错"或口头确认不能替代该证据。
验证方式：收工时检查测试输出。
原因：_（在第一次违反时填入具体事件）_

### 规则：测试定义需求，不是实现定义测试

MUST 当测试失败时修改实现代码，不得反过来修改测试的预期值。
验证方式：收工时 `git diff` 检查测试文件；若预期值有变更，必须附带需求变更记录。
原因：_（在第一次违反时填入具体事件）_

### 规则：不得静默扩大范围

MUST 在修改超出 CURRENT.md "进行中"范围前，先报告变更理由并等待确认。
验证方式：收工时核对实际修改文件列表与预定范围。
原因：_（在第一次违反时填入具体事件）_

<!-- 新规则在此追加。每条规则必须有：
     - MUST 或 SHOULD + 可观察的要求
     - 验证方式
     - 原因（具体的失败事件）
     无法用命令检查的偏好用 SHOULD，不伪装成 MUST。 -->
