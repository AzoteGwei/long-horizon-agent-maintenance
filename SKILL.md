---
name: long-horizon-agent-maintenance
description: 为跨多会话、多人或多 Agent 接力的艰难项目建立可持续推进协议：把稳定规则、当前状态、证据与失败史外化，设计可复核的进度闸门，并生成启动与收工模板。适用于长期开发、迁移、重构、审计、研究和验证；不适用于一次会话即可完成且无需交接的简单任务。
---

# 长程艰难项目的 Agent 维护协议

这是一套“项目记忆与控制面”设计方法，不是某个领域的工作手册。它适用于代码、数据、研究、迁移、审计、形式化验证以及混合项目。领域工具和验收方式由项目决定；本 skill 只规定如何让工作在会话之间可靠地继续、纠错和交接。

## 先判断是否值得启用

满足下列任一条件，就应至少采用轻量版本：

1. 工作会跨越多个会话、执行者或 worktree，接棒者可能没有当前对话上下文；
2. 任务包含许多依赖、隐含决定或容易重复踩的失败路径；
3. “做完”需要测试、审阅、复现实验、用户验收或其他外部证据，而不能凭执行者自述；
4. 中断、回滚、换人或换模型后，重新理解项目的成本很高。

单会话的小修复不必建立完整文档树。不要把模板数量本身当作成熟度指标；目标是降低下一次决策的成本。

可按风险分级启用：

- **轻量**：一份规则文件、一份状态快照、每轮日志；适合几天内完成的跨会话任务；
- **标准**：再加 findings/实验记录、生成进度和正反向回归；适合多人接力或存在客观验收的项目；
- **审计型**：增加不可变/外部证据、版本化决策、探针反例、基线台账和定期文档健康检查；适合高代价错误、合规、研究或安全关键工作。

先从能坚持的最低级别开始；重复失败或接力成本上升时，再把经验升级为更强的结构。

启用 Skill 时，先做一次初始化决策，并把结果写入规则文件或状态文件：将选择的控制面具体的描述文件位置及作用、维护级别、实际启用的文件，以及“每次对话”“收工”“阶段切换”和“定期”的具体含义。频率优先用事件触发（如每次收工、发生阻塞或切换里程碑）；确需日历周期时写明具体周期。短期项目可以只维护规则和状态，明确不建立 findings、索引、反向闸门或文档健康检查；未启用的项目材料不应在后续工作中变成默认义务。

## 项目拓扑与文档归属的取舍

这些文档的逻辑职责应保持稳定，但它们由哪个 Git 仓库追踪、是否公开以及放在什么目录，必须服从项目现状和用户意愿。先检查是否已有代码仓库、workspace 仓库、`.project/`、`docs/`、`AGENTS.md` 或 `CLAUDE.md`；不要因为模板存在就迁移或新建第二份事实来源。如果涉及到多个仓库，请明确在你的文档中写出 Agent 应当在每个仓库中以何种身份提交。

常见有三种控制面模式：

**A：`.project` 本地控制面**

```text
code-repo/
└── .project/
```

适合个人探索、短期接力或过程资料暂不公开的阶段。它可以用 `git/info/exclude` 排除，但“未提交”不等于有版本控制、备份或保密性。若采用此模式，交接 prompt 必须显式指出路径，并说明资料的备份责任。

**B：`.project` 独立文档仓库**

```text
code-repo/
└── .project/
    └── .git/
```

适合需要保留完整文档历史、但不希望过程记录进入代码仓库的项目。嵌套 Git 会带来明确成本：外层仓库默认看不到其中的文件，新 clone 不会自动得到它们，Agent 必须分别检查和提交两个仓库。除非用户接受这些成本，不要擅自采用；不要把嵌套仓库误称为 submodule。

**C：workspace 控制面**

```text
workspace/
├── .git/
└── code-repo/
```

workspace 仓库追踪 `.project` 或 `docs/`，代码仓库保持独立。它适合外部上游代码、多 Agent 长期接力、需要观察多个仓库，或实验与失败史已经是工作区资产的项目。workspace 与 code-repo 的 Git 状态、提交边界和验证命令必须分别记录。

没有明确偏好时，单仓库的小型长期任务可从 A 开始；需要独立文档历史时再考虑 B；已有独立代码仓库或多人/多 Agent 长期协作时优先考虑 C。这个默认只是建议，不覆盖用户选择和已有项目约定。

A、B、C 不是三套不同的内容模型。无论采用哪种模式，都保持同一组逻辑层：规则（`AGENTS.md`/`CLAUDE.md`）、当前状态（`STATE.md`/`CURRENT.md`）、findings/实验/决策、session 日志、交接入口和可再生的派生材料。

### 受控迁移

从 A 或 B 迁移到 C，或把本地资料提升为正式 `docs/`，应视为一次有记录的项目变更，而不是目录复制：

1. 先冻结原位置的事实来源，盘点每个文件的生命周期（本地、workspace 共享、项目公开或生成文件）。
2. 把稳定规则、已确认的 finding、必要状态和再生命令迁移到目标位置；临时日志、私有材料和已过期草稿可以保留在原控制面。
3. 保留来源、迁移日期和代码基线；原位置留下指针，迁移后不得双写同一事实。
4. 检查索引、链接、启动 prompt、Git 边界和文档再生流程，确认新 clone 或新 Agent 能找到唯一入口。

除非用户明确要求或项目阶段已经成熟，不要主动执行迁移。迁移计划本身应写入 finding 或决策记录，并在状态文件中标明停点和恢复动作。

## 不可替代的核心：断言、证据、决策分开

执行者的报告只是断言。完成状态必须绑定到与报告分离、可复现且尽量由独立工具、审阅者或用户确认约束的证据，例如测试结果、构建产物、评审记录、实验输出、可复现命令或外部系统状态。不要把“证据文件存在”本身当成证据：记录其生成命令、版本和信任边界。

每条重要结论都应标明：

- **状态**：已证实、已实现待验证、假设、待判定、被推翻、阻塞；
- **证据**：命令、文件、提交、实验、审阅者或用户确认；
- **范围**：适用条件、未覆盖部分和时间/版本；
- **下一动作**：若未完成，给出可执行的判定或推进动作。

没有证据时写“尚未判定”，不要用“应该”“看起来没问题”填空。没有天然神谕的任务，应尽早建立一个足够弱但可重复的判据；随后用故意失败的探针、反例或对照组检查这个判据不会把明显错误判成成功。

## 六类文档各司其职

项目已有命名和目录优先。没有约定时，可采用下表；`AGENTS.md` 与 `CLAUDE.md` 是同一类“规则文件”，按宿主工具的优先级放置，不要为了套模板而新增冲突文件。

| 文档 | 保存什么 | 不保存什么 | 更新节奏 |
|---|---|---|---|
| `AGENTS.md` / `CLAUDE.md` | 稳定、跨会话有效的 MUST/禁止事项、信任边界、验证入口、提交与安全规则 | 当前数字、长篇历史、某一轮的临时计划 | 规则改变时 |
| `docs/STATE.md`（或项目既有状态文件） | 当前阶段、已完成、进行中、未完成、阻塞、最近证据、下一步 | 完整聊天记录、可由脚本生成的明细、已经失效的旧计划 | 按初始化时定义的事件 |
| `docs/findings/`（或 `decisions/`, `research/`） | 一个发现/实验/决策/反例及其证据、影响和状态；被推翻的结论保留并加醒目标记 | 只有一句“下次注意”的告诫、无出处的传闻 | 仅在启用后，于发生新发现、实验、决策或推翻时记录 |
| `docs/sessions/` 或 `docs/session-log/` | 按轮追加的工作日志：做了什么、失败了什么、为何失败、证据、交接 | 把当前状态当日志重复粘贴 | 仅在启用后，按定义的工作单元结束时追加 |
| 生成文档/台账 | 由脚本计算的进度、索引、统计、溯源 | 手工复制的数字和可编辑的第二事实来源 | 由脚本再生 |
| `HANDOFF.md` / `START-PROMPT.md` | 环境入口、常用命令、首次启动与继续工作的 prompt | 随进度变化的事实、未验证的承诺 | 环境或协议改变时 |

如果仓库已有 `PROGRESS.md`、`traceability.md`、`ledger.json` 等文件，先确认其来源。注明“勿手改”，让脚本成为唯一事实来源；人工文档只链接或引用生成结果。

需要创建或重整这些文件时，读取 [references/project-memory-templates.md](references/project-memory-templates.md)。其中提供规则文件、状态、finding、session 和交接自检的最小模板；只采用项目实际需要的部分。

## 编写规则文件：把事故变成可执行的边界

只有在用户授权、项目约定或当前工作明确要求时，才创建或修改根目录规则文件。修改前必须读取已有的规则文件、贡献指南、CI/构建说明和适用的上级规则；新文件不能削弱它们，冲突必须报告。

规则文件应短而硬，优先写会导致系统性损害的事项：

1. **信任边界**：哪些目录/接口是允许假设的，哪些必须真实实现或复核；
2. **成功判据**：哪条命令、产物或人工确认才算完成；运行结束、无报错或 Agent 说成功都不算；
3. **禁止的捷径**：不得删测试、放宽验收、伪造数据、绕过审阅、静默扩大范围或用未经验证的引理/桩/模拟替代真实行为；
4. **变更前置**：开始修改前必须读取哪些文件、检查什么状态、先报告哪些差异；
5. **收工协议**：必须再生哪些文件、更新状态、追加日志、运行哪些闸门、如何记录未完成项；
6. **安全与权限**：敏感信息、外部服务、破坏性操作和提交边界；
7. **提交约定**：遵循既有规范；没有规范时使用清晰的 Conventional Commits，不把规划编号冒充提交内容。

每条禁令都应回答“防哪个具体失败”，每条 MUST 都应有可观察的检查。无法检查的偏好写成 SHOULD 或放入手册，不要伪装成铁律。规则文件不应成为百科全书：命令细节、领域知识和长篇历史放到 `HANDOFF`、playbook 或 findings。

推荐的规则条目形状：

```markdown
## 规则：完成状态不可由执行者自证

MUST 以 `<命令或证据>` 复核，结果保存于 `<路径>`。
任务成功、无报错或口头确认不能替代该证据。
违反原因：<曾经发生的系统性失败>。
```

## 维护状态、发现和会话日志

### 状态快照

保持一个短小、可在几分钟内读完的当前状态文件，至少包含：

```markdown
# 当前状态
- 状态：
- 当前阶段/里程碑：
- 最近更新：
- 基于的提交或版本：

## 已完成
## 进行中
## 尚未完成
## 阻塞事项
## 验证结果
## 下一步
## 交接备注
```

“下一步”必须是最小可执行动作，优先给出可复制命令、目标文件和预期信号；不要只写“继续完善”“调查问题”。状态只保留当前有效摘要，详细推理链接到 finding 或 session。只有初始化时启用了文档健康检查，才按已定义的触发点或周期执行；短期项目可以不做。

### Findings / 实验 / 决策

一份记录聚焦一个问题，建议包含：问题与背景、假设、最小复现实验或判定命令、观察结果、结论状态、影响范围、未解决分支、来源与日期。故意失败的实验尤其有价值：它证明闸门确实能抓住错误。

不要删除被推翻的结论；在文件顶部加 `RETRACTED`/“已推翻”横幅，链接到新证据。只有项目启用了索引，才维护索引并按已定义的触发点对账。

### Session 日志

若初始化时启用 session 日志，按定义的工作单元追加，不追求漂亮的日报。每轮至少写：目标、实际改动、验证命令与结果、失败尝试及原因、做出的决定、未完成/阻塞、下一轮可直接执行的动作。失败史不能只留在聊天里；它是防止 Agent 重新发明死路的高价值资产。

## 进度闸门：正向、反向、基线三者并用

- **正向闸门**：应该成功的测试、构建、迁移、检查必须成功；
- **反向闸门**：应该失败、拒绝、保持未实现或保持不确定的项目必须继续失败/未实现；其消失或变绿可能表示过度承诺；
- **基线闸门**：记录已知结果，检查未授权的退化、意外增减和环境漂移。

只有启用了反向闸门的项目才需要维护反向清单；启用后，名单项找不到、被静默删除或检查器没有实际读取，都应失败。红灯时先读条目背后的理由，不得通过删名单、改期望值或降低断言让闸门变绿；若确需改变承诺，先写版本化决策并取得相应批准。

对重复且有价值的检查，可以在初始化或后续维护决策中指定由脚本、CI 或收工协议执行；不要把“逐步”解释成所有项目都必须自动化。数字从脚本或原始产物再生，不从记忆或手抄表格读取。不同证据层不要混为一个数字：例如静态阅读、单元测试、集成测试、形式化证明和用户验收应分别报告。

## 从失败到结构性修复

每次事故结束时，必须选择至少一种结构化结果：

- 变成机器可检查的闸门；
- 变成规则文件中的明确禁止或 MUST；
- 变成可重放的最小探针/反例；
- 变成 finding 中带出处的建模限制或决策。

只写“以后小心”不算修复。若一个失败反复出现，应把触发条件加入检查器或模板；若检查器本身被证明不可靠，应记录该事实并修正其信任边界。

## 上下文与来源纪律

长规范、日志、转储和生成文件不要整份塞进上下文。优先使用带章节/行号/版本坐标的切片、搜索或脚本；日志只读相关轮次；台账只读报告。每条外部事实保留来源、版本和查询日期。上下文保护与可复核出处是同一个设计目标：下一棒既能找到证据，也不会被无关原料淹没。

## 会话工作流

执行者按初始化时定义的工作单元执行以下顺序；未启用的材料和检查跳过：

1. 读取适用规则文件、状态快照和当前阶段所需的 handoff/findings 索引；检查 Git、环境和可用验证命令。首次启用 Skill 时，先完成上文的初始化决策并落盘。
2. 报告文档与代码/产物是否一致、活动范围、准备修改的文件、阻塞和需要批准的取舍；冲突不得静默选择。
3. 选择一个可在本轮完成并验证的最小工作单元；先建立或复用证据，再修改实现。
4. 运行与风险匹配的正向、反向和基线检查；将“未运行”明确写成未验证。
5. 记录失败和决定，按配置更新状态；只再生已声明的派生文档，只追加已启用的 session 日志，并只检查已启用的索引/链接。
6. 报告已完成、未完成、阻塞、证据、下一步和可能需要用户决定的事项。未满足收工协议不宣称完成。

规则、状态或目录不存在时，先采用项目已有约定；只有在授权范围内补齐最小结构。不要为了“整齐”重命名或迁移大量文档。

## 首次启动与继续工作的 prompt

把项目的真实路径、规则文件名和验证命令替换进去。Prompt 必须显式指向文件，不要假定 Agent 会自动发现隐藏目录。

**首次启动：**

```text
你是这个长期项目的执行 Agent。开始任何修改前，先读取：
<规则文件路径>、<状态文件路径>、<handoff/阶段计划>，并查看相关 findings 索引。
检查代码/产物、Git 状态、环境和可用验证命令；先报告当前状态与材料是否一致、
本轮拟处理的最小范围、阻塞和需要确认的取舍。若状态文件已有明确、获授权且未阻塞的下一步，
报告后直接执行；只有遇到会改变范围、承诺、风险或外部状态的真实决策点才停下来询问。
所有“完成”都必须附可复核证据；不要静默扩大范围、削弱验收或重复已记录的失败路径。
结束前执行项目收工协议：运行闸门、再生生成文档、更新状态、追加本轮日志（含失败原因），
并报告已完成、未完成、阻塞、证据和下一步。
```

**继续工作：**

```text
读取 <规则文件路径>、<状态文件路径>、<handoff/阶段计划>，从状态文件记录的活动阶段继续。
先核对当前代码/产物、Git 状态和最近证据；若发现漂移、退化或冲突，先停在报告并记录，
不要自行改写规则或需求。否则直接选择状态文件“下一步”中的一个最小动作，无需为例行步骤等待确认；
完成并验证后按收工协议落盘：
更新状态、追加 session 日志、再生派生文档、运行正向/反向/基线闸门，并说明下一步。
```

能力差异明显时，可给低能力执行者增加读取顺序、禁止事项、具体命令和预期输出；强执行者只需文件指针。Prompt 是接力层的临时脚手架，稳定机制仍应落在规则、证据和自动闸门中。

## 何时不应照搬

不要把形式化验证中的术语（如 `closed`、双堆、用户引理）写成通用硬规则；应替换为该项目真实的判据。不要要求所有项目建立同样的目录、日志编号或里程碑模型；先遵循现有约定。不要让文档协议阻止低风险的小修复，也不要把“写了状态文件”当作交付本身。

这套 skill 的长期内核只有四件事：把信任交给外部证据、把上下文交给盘上状态、把失败转成结构、把下一步写到别人可以执行。其余模板都应随项目规模和 Agent 能力增删。
