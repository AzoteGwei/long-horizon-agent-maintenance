---
name: long-horizon-agent-maintenance
description: 为需要跨多个 session 推进的项目量身设计控制面——执行协议、状态追踪、失败记录、收工流程，以及防止进度停滞的 supervisor 循环。这是一个设计时 Skill：由用户在项目初始化或偏移修正时主动触发，产出精简的两层循环协议写入项目文件，然后退场。适用于绿地开发、重大重构、迁移、研究和形式化验证等长程项目；不适用于一次 session 即可完成的简单任务。
---

# 长程项目控制面设计

这是一个 meta-skill。由用户主动触发，为当前项目**设计**一套量身定制的控制面和循环协议，然后退场。

产出物——写入项目文件的规则、初始化的状态文件、两层循环的协议和 bootstrap prompt——才是每个后续 session 真正执行的东西。本 Skill 本身不在日常 session 中加载，也不由 Agent 自行决定启用。

## 三条底线

控制面的一切设计都服务于这三条原则。如果一个流程不能追溯到其中之一，它可能不需要存在。

**失败必须变成结构。**
"下次注意"不算修复。每次失败至少变成以下一种：规则文件中的 MUST 或禁止、可自动运行的验证检查、可重放的最小反例、或 findings 中带出处的记录。同一个错误能犯第二次，说明结构缺了一块。

**状态活在磁盘上，不活在上下文里。**
Agent 的上下文窗口是临时的、昂贵的、每次 session 会清零的。进度、决策、失败原因、下一步——对下一个 session 有价值的一切都必须写入文件。只存在于对话历史中的信息等于已经丢失。

**完成由独立证据判定。**
Agent 说"做完了"只是断言，不是事实。完成状态要绑定到与自述分离的证据：测试通过、构建成功、实验输出、审阅记录、用户确认。在常规项目中这就是测试驱动；在形式化验证或安全关键项目中，证据标准更高——但"由独立手段验证"这个原则不变。

## 何时触发

本 Skill **只在用户主动要求时触发**，不由 Agent 自行决定启用。

**初始化**——用户要求为一个长期项目建立控制面。典型场景：

- 工作会跨多个 session，下一个 session 没有当前对话的上下文；
- 任务包含大量隐含决定或容易重复踩的失败路径；
- 完成需要外部证据，不能凭 Agent 自述；
- 中断后重新理解项目的成本很高。

**偏移修正**——用户判断 session 协议已经漂移，要求重新对齐。典型信号：规则不再反映现实、状态文件过时、失败在重复发生、进度在放缓。

单 session 能完成的小任务不需要控制面。建立文档不等于取得进展。

## 两层循环

控制面产出两个循环协议，对应两种角色：

### Session 循环（内层）

每个 session agent 在干净的上下文中执行同一套固定流程：

```
读规则 + 状态 → 核对现实 → 选取可执行的下一步 → 执行 → 收工 → 退出
```

Session agent 不区分"首次"和"继续"——状态文件本身就告诉它现在处于什么阶段。如果 `CURRENT.md` 是初始状态，它就从头开始；如果有进行中的工作，它就接着做。每次都是同一个 prompt，同一套流程。

Session agent 只看得到"本轮该干什么"。它没有能力也不需要回答"项目整体还在前进吗"。

### Supervisor 循环（外层）

Supervisor 是跨 session 的趋势观察者，由人类启动：

```
读自己的状态（SUPERVISOR.md）和 CURRENT.md 的"下一步"
  → spawn 一个干净的 session agent（用 HANDOFF.md 中的 prompt）
  → session agent 完成后汇报结果
  → 根据汇报更新趋势追踪
  → 检查 CURRENT.md 中"下一步"的范围是否在萎缩
  → 判断：继续 spawn / 介入调整 / 上报用户
  → 循环
```

Supervisor 不需要主动读 `CURRENT.md` 的全部内容来了解产出——session agent 完成后自然会汇报做了什么。Supervisor 读 `CURRENT.md` 主要是看**"下一步"的范围变化**：如果每轮的"下一步"越来越小、越来越碎，这是需要介入的信号。

介入方式可以是在下一次 spawn 时给 session agent 额外的方向提示，也可以直接修改 `CURRENT.md` 的"下一步"来扩大工作单元的粒度。

**Supervisor 不替 session agent 干活。** 它的职责是观察、判断和转交。类似 Erlang supervisor tree：worker 的状态不污染 supervisor 的判断，supervisor 只在必要时介入。

Supervisor 自己也要落盘——状态写在 `.project/SUPERVISOR.md`，追踪的不是代码细节，而是跨 session 的趋势和决策。

#### 进度停滞检测

Supervisor 在每个 session 结束后检查以下信号。**满足任一即上报用户**，由用户决定是推进、调整还是继续：

- **代码变更递减**：最近 3 个 session 的代码净变更持续下降，而文档/findings 变更在上升——在高效产出文档而不是代码。
- **工作单元萎缩**：每个 session 的实际改动范围在缩小——"最小可验证单元"没有下限，会无限细化。
- **"下一步"重复**：连续 2 个 session 的"下一步"实质相同——上一轮要么没做完，要么做了等于没做。
- **Milestone 超期**：当前 Milestone 已花费的 session 数超过初始预估的 2 倍。
- **Session agent 主动上报**：报告了阻塞、环境问题或设计层面的质疑。

这些信号触发的是**上报**，不是自动行为。Supervisor 不自行决定跳过 Milestone 或放宽标准；人类做这个决定。

`scripts/check-trend.sh` 可辅助检测：它从 git log 统计最近 N 个 commit 的代码 vs 文档变更行数，并输出粗略的趋势警告。Supervisor 或人类均可使用：

```bash
bash .project/scripts/check-trend.sh 5   # 最近 5 个 commit
```

#### 不支持子 Agent 的工具

如果 code agent 不支持 spawn 子 Agent（如 OpenCode 等），人类就是 supervisor。每次手动输入"继续"本质上就是在做 supervisor 的判断。此时 long-horizon 的价值退化为"至少 session 协议和状态文件是对的"，进度停滞检测只能靠人类自己观察。

用户指南中应提醒人类注意与上文相同的信号——这部分由用户自己编写。

## 文件结构

### 项目知识层：`CLAUDE.md`（或 `AGENTS.md`）

提交到仓库，对所有协作者可见。存放**项目本身的隐含知识**——不写在代码里但每个参与者都应知道的事：

- 技术栈约定（"这是 pnpm 项目，不要用 npm 或 yarn"）
- 环境管理（"用 asdf 管理 Node 版本，见 .tool-versions"）
- 构建和测试入口（"`pnpm test` 跑测试，`pnpm lint` 做检查"）
- 提交规范、分支策略、CI 注意事项

这一层**不放**当前进度、session 日志、Agent 的中间推理，也**不放**指向 `.project/` 的指针——`.project/` 是 iykyk 的东西，知道它的 Agent 通过 bootstrap prompt 找到它；不知道的不需要看到一个困惑的指针。

### 工作记忆层：`.project/`

通过 `info/exclude` 排除出 Git，仅本地可见。存放 Agent 的工作记忆：

```text
.project/
├── RULES.md          # session agent 的执行协议
├── CURRENT.md        # 当前状态快照（session agent 读写）
├── SUPERVISOR.md     # supervisor 的趋势追踪（supervisor 读写）
├── findings/         # [标准+] 一个文件一个发现/实验/决策
├── sessions/         # [标准+] 按工作单元追加的日志
└── HANDOFF.md        # session agent 的 bootstrap prompt（稳定，每次相同）
```

各文件职责：

| 文件 | 放什么 | 不放什么 |
|---|---|---|
| `RULES.md` | 稳定的 MUST / 禁止、信任边界、验证要求、收工流程 | 当前进度、长篇历史、临时计划 |
| `CURRENT.md` | 当前阶段、已完成/进行中/未完成/阻塞、最近证据、可执行的下一步 | 完整推理过程、可由脚本生成的明细 |
| `SUPERVISOR.md` | 跨 session 趋势、每轮变更统计、停滞检测记录、上报和用户决策 | 代码细节、session 的完整日志 |
| `findings/` | 一个发现或决策及其证据、影响、状态；被推翻的结论保留并标记 | 无出处的告诫 |
| `sessions/` | 做了什么、失败了什么及原因、证据、决定、下一步 | 状态快照的重复粘贴 |
| `HANDOFF.md` | session agent 的 bootstrap prompt | 随进度变化的内容（那些属于 CURRENT.md） |

`HANDOFF.md` 的内容是**稳定的**——它存的是 session agent 每次都用的同一个 prompt，不随项目进度变化。变化的是 `CURRENT.md`，session agent 读 prompt 之后自然会去读它。

Supervisor 的 bootstrap prompt 不在 `.project/` 里——它由用户存放在自己的指南中，因为 supervisor 是由人类启动的。

如果已有 `.project/AGENTS-INSTRUCTION.md`（如 requirements-interrogator 建立的），将其内容合并进 `RULES.md` 或直接复用。文件名服从项目已有约定。

## 初始化流程

### 第一步：评估现状

先看项目里已经有什么，不要从零套模板。

- 检查是否已有 `CLAUDE.md`、`AGENTS.md`、`.project/`、`docs/` 或其他状态文件。
- 检查仓库结构、Git 状态、CI、现有测试和验证命令。
- 如果已有 `.project/` 结构，在其上扩展，不建立平行结构。

### 第二步：选择控制级别

从能坚持的最低级别开始。重复失败或交接成本上升时再升级。

**轻量**（几天到几周的跨 session 任务）：
`CLAUDE.md` + `RULES.md` + `CURRENT.md` + 每轮收工时追加简要记录。不需要独立的 findings、session 日志或自动化验证。

**标准**（多周以上，或存在客观验收标准）：
加上 `findings/` 记录重要发现和决策、独立的 session 日志、正向和反向验证检查。

**高风险**（形式化验证、安全关键、合规）：
加上基线验证检查和回归对照、不可变或外部证据要求、定期文档健康检查。

把选择的级别、实际启用的文件清单、"收工"的触发条件写入 `RULES.md`。**未启用的组件不应在后续 session 中变成默认义务。**

### 第三步：写入项目知识

在 `CLAUDE.md`（或 `AGENTS.md`）中补充技术栈、环境、构建测试的关键约定和提交规范。不放 `.project/` 指针。

### 第四步：创建工作记忆

在 `.project/` 下建立控制面文件。从 `templates/zh/` 或 `templates/en/` 复制模板（根据项目语言选择），填入项目信息。

模板清单：
- `RULES.md` — 执行协议，含示例规则条目
- `CURRENT.md` — 状态快照
- `SUPERVISOR.md` — Supervisor 趋势追踪
- `HANDOFF.md` — Session bootstrap prompt + 环境入口
- `finding.md` — 单个 finding（复制到 `findings/` 下使用）
- `session-log.md` — Session 日志条目（追加到 `sessions/` 下的文件中）

模板中的 HTML 注释（`<!-- -->`）是填写指引，填入实际内容后可以保留或删除。

### 第五步：设置隐私保护

运行 `scripts/init-privacy.sh`（幂等）：

```bash
bash .project/scripts/init-privacy.sh
```

脚本会自动完成：确认 Git 仓库 → 检查已追踪文件（有则警告用户） → 检查是否已忽略 → 解析 `info/exclude` 路径 → 幂等追加 `.project/` → 验证生效。

使用 `info/exclude`（本地私有），不修改 `.gitignore`（项目公共配置）。如果有已追踪文件，脚本会输出路径并提醒处理，但不自行 `git rm`。

**独立文档仓库**：如果项目需要 `.project/` 的版本历史，可在其内 `git init`。成本：外层仓库看不到、新 clone 不包含、必须分别操作两个仓库。除非用户主动要求，不采用。

### 第六步：设计 session 协议

写入 `RULES.md` 的 session 协议，每个 session agent 每次都执行同一套流程：

**开工：**
- 读取 `RULES.md` 和 `CURRENT.md`
- 核对代码与状态是否一致
- 如有漂移或冲突 → 报告，不自行选择
- 如无异常 → 从"下一步"选取可执行动作，直接开始
- "下一步"已明确且未阻塞时，不停下来问"继续吗？"

**执行：**
- 完成一个可在本 session 内验证的工作单元
- 不静默扩大范围、不削弱验收标准、不重复已记录的失败路径
- 未运行的检查标记为"未验证"

**收工：**
- 运行验证检查（按已启用的级别）
- 更新 `CURRENT.md`：已完成、未完成、阻塞、证据、下一步
- [标准+] 追加 session 日志，含失败尝试及原因
- 报告结果；未满足收工流程不宣称完成

"下一步"必须是可直接执行的最小动作——带目标文件、预期信号、可复制的命令。"继续完善"、"调查问题"不是下一步。

不区分"首次启动"和"继续工作"。状态文件本身就告诉 agent 当前处于什么阶段——如果是初始状态就从头开始，如果有进行中的工作就接着做。一个 prompt，每次都一样。

### 第七步：生成 prompt 并输出给用户

初始化完成后，在控制台中输出两个 prompt 供用户使用：

#### Session Prompt（单次驱动）

存入 `.project/HANDOFF.md`，同时输出给用户。用于手动驱动单次 session，或由 supervisor 在 spawn 子 Agent 时使用。

```text
读取 .project/RULES.md 和 .project/CURRENT.md。
核对代码和 Git 状态与 CURRENT.md 是否一致；如有漂移或冲突，先报告。
从"下一步"中选取可执行动作，直接开始，不为例行步骤等待确认。
完成后执行 RULES.md 中定义的收工流程：
更新 CURRENT.md、运行验证检查、报告已完成/未完成/阻塞/证据/下一步。
```

每次都是这同一个 prompt。

#### Supervisor Prompt（循环驱动）

输出给用户，由用户存放在自己的指南中（如 README）。不写入 `.project/`。

```text
你是这个项目的 Supervisor。你的职责是驱动项目持续推进，不是自己写代码。

启动前读取 .project/SUPERVISOR.md 和 .project/CURRENT.md 的"下一步"。

每一轮：
1. 用 .project/HANDOFF.md 中的 prompt 启动一个子 Agent 执行一次 session。
2. 子 Agent 完成后，根据它的汇报更新 SUPERVISOR.md 的趋势表。
3. 检查 CURRENT.md 的"下一步"——范围是否在缩小？
4. 检查停滞信号：代码变更递减、工作单元萎缩、"下一步"重复、Milestone 超期。
5. 如有停滞信号或子 Agent 上报了阻塞/设计问题 → 停止循环，向用户汇报趋势数据。
6. 如无异常 → 继续下一轮。

你可以在 spawn 下一个子 Agent 前，通过修改 CURRENT.md 的"下一步"或在 prompt 中补充方向提示来调整粒度。
不要自行跳过 Milestone、放宽验收标准或替代用户做设计决策。
```

用户拿到这两个 prompt 后：
- 有子 Agent 能力 → 用 supervisor prompt 启动自动循环
- 没有子 Agent 能力 → 每次手动粘贴 session prompt，自己充当 supervisor

## 偏移修正流程

用户判断需要重新对齐时，重新加载本 Skill：

1. **读取**全部控制面文件、`SUPERVISOR.md` 的趋势数据和最近的 session 日志。
2. **诊断**：
   - 规则有没有被反复违反但未修正？
   - 状态文件是否与代码现实脱节？
   - 重复出现的失败有没有变成结构？
   - Session 协议有没有成为空文的步骤或缺失的步骤？
   - 进度停滞检测的阈值是否还合理？
   - 控制级别还合适吗？
3. **修正**：更新规则、调整协议、升降级控制级别、校准停滞检测阈值。
4. **记录**：把修正作为 finding 或决策写入。

## 规则文件写法

规则应短而硬，优先写会造成系统性损害的事项。每条规则回答："防哪个具体失败？"和"怎么检查？"

无法用命令检查的偏好写成 SHOULD，不伪装成 MUST。规则文件不是百科全书——命令细节放 HANDOFF，领域知识放 findings。修改前必须读取已有规则和贡献指南；新规则不能削弱它们。

**规则条目的推荐形状：**

```markdown
## 规则：<一句话说清防什么>

MUST <可观察的要求>。
验证方式：<命令或检查步骤>。
原因：<曾经发生的具体失败>。
```

**示例——Agent 自证完成：**

```markdown
## 规则：完成状态不可由执行者自证

MUST 以 `make test && make lint` 的通过结果复核，日志保存于 `.project/evidence/`。
"任务成功"、"无报错"或口头确认不能替代该证据。
原因：Session 12 中 Agent 报告"重构完成"，但实际有 3 个测试被静默删除。
```

**示例——不得修改测试来适配实现：**

```markdown
## 规则：测试定义需求，不是实现定义测试

MUST 当测试失败时修改实现代码，不得反过来修改测试的预期值来让 CI 通过。
验证方式：收工时 `git diff` 检查测试文件；若预期值有变更，必须附带需求变更记录。
原因：Session 5 中 Agent 把 3 个断言的预期值改成了实现的实际输出——等于让实现反过来定义需求。这和在证明中用 sorry 跳过未证引理一样，只是让红灯变绿，没有解决问题。
```

**示例——范围蔓延：**

```markdown
## 规则：不得静默扩大范围

MUST 在修改超出 CURRENT.md "进行中"范围前，先报告变更理由并等待确认。
验证方式：收工时核对实际修改文件列表与预定范围。
原因：Session 8 中 Agent 在修 bug 时顺手重构了 3 个不相关模块，引入新 bug。
```

## Findings、Session 日志和验证检查

### Finding

一份 finding 聚焦一个问题。格式见 `templates/<lang>/finding.md`。

关键规则：被推翻的 finding 不删除——顶部加 `⚠️ 已推翻` 标记，链接新证据。故意设计的失败实验（反例、探针）尤其有价值：它证明验证检查确实能抓到错误。

### Session 日志

按工作单元追加。格式见 `templates/<lang>/session-log.md`。

**失败记录是最高价值的部分**——它防止下一个 session 重走死路。不要把失败只留在聊天记录里。

### 三类验证检查

- **正向检查：** 应该通过的必须通过——测试、构建、迁移脚本。
- **反向检查：** 应该失败的必须继续失败——未实现的功能应报错、非法输入应被拒绝。突然通过可能意味着验收标准被放宽了。
- **基线检查：** 记录已知结果，定期重跑，检测意外退化或环境变化。

检查变红时先读背后的理由，不得通过删条目或改预期值来变绿。轻量级项目只用正向检查就够。

## 反模式

**文档即进度。** 建了完整的文件结构，代码没推进。文件是手段。

**无限问询。** 每做一步都停下来问"继续吗？我先做 X"——如果状态文件有明确的下一步，直接执行。你已经安排好了，用户也没法说"不"，这个确认就是浪费。

**规则膨胀。** 规则文件几百行，每条都 MUST。保留高价值的、可检查的条目，其余降级或移走。

**状态变聊天记录。** 把完整推理过程塞进 CURRENT.md。状态文件只保留当前有效的摘要。

**照搬模板。** 不管项目规模，全量启用所有组件。先从轻量开始。

**精致的磨洋工。** 每个 session 都在高效产出 findings 和日志，代码变更趋近于零。文档也是工作，但文档不是交付物。Supervisor 的停滞检测就是为了捕获这个模式。

## 长期内核

所有模板和流程都可以随项目增删，但四件事不变：

1. 把失败转成结构，不转成"下次小心"
2. 把状态交给磁盘，不交给上下文窗口
3. 把完成交给证据，不交给自述
4. 把下一步写到任何 session 可以直接执行
